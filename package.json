{
  "name": "jsonresume-theme-boilerplate",
  "version": "0.1.2",
  "description": "Boilerplate theme for JSON Resume",
  "author": "Richard Wardle",
  "repository": {
    "type": "git",
    "url": "https://github.com/rwardle/jsonresume-theme-boilerplate"
  },
  "scripts": {
    "export": "npx resume export --format pdf --theme . resume.pdf",
    "serve": "npx resume serve --theme ."
  },
  "license": "MIT",
  "dependencies": {
    "handlebars": "^4.7.7"
  },
  "devDependencies": {
    "resume-cli": "^3.0.6"
  },
  "overrides": {
    "puppeteer": "^13.4.0"
  }
}
