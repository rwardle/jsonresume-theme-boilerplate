{{#if resume.skills.length}}
<section id="skills">
	<div class="title">
		<h2 class="title-text">Skills</h2>
		<div class="title-divider"></div>
	</div>
	
	{{#each resume.skills}}
	<div class="item">
		{{#if name}}
		<div class="name">
			<strong>{{name}}</strong>
		</div>
		{{/if}}
		{{#if keywords.length}}
		<div class="keywords">
			{{#each keywords}}
			{{.}}{{#if @last}}{{else}},{{/if}}
			{{/each}}
		</div>
		{{/if}}
	</div>
	{{/each}}
</section>
{{/if}}